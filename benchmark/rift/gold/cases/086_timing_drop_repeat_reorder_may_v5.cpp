/*
 * RIFT-GOLD-086: timing_drop_repeat_reorder, variant 5, MAY_INFLUENCE
 * Generated by RIFT mechanical gold generator 1.0.0.
 * Ground truth is template-derived; it is not a human label.
 */
#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>

static int read_arg(int argc, char **argv, int index, int fallback) {
    if (index >= argc) {
        return fallback;
    }
    char *end = NULL;
    long value = strtol(argv[index], &end, 10);
    return (end == argv[index]) ? fallback : (int)value;
}
struct NeutralTimeline { int delay; int delivered; int repeats; int order; };

    int main(int argc, char **argv) {
        /* RIFT_SOURCE:source_primary */
        int source_primary = read_arg(argc, argv, 1, 1);

            /* RIFT_SOURCE:source_sequence_gate */
            int source_sequence_gate = read_arg(argc, argv, 2, 1);


        /* RIFT_NODE:node_timeline */
        struct NeutralTimeline node_timeline = {5, 1, 2, 0};
        node_timeline.delivered = source_sequence_gate ? source_primary : 1;
        /* RIFT_NODE:node_event_value */
        int node_event_value = node_timeline.delivered;
        /* RIFT_AP:ap_primary */
        int ap_primary = (node_event_value != 0);
        printf("AP_primary=%d\n", ap_primary);
        return 0;
    }
