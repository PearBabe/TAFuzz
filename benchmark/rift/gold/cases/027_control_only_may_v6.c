/*
 * RIFT-GOLD-027: control_only, variant 6, MAY_INFLUENCE
 * Generated by RIFT mechanical gold generator 1.0.0.
 * Ground truth is template-derived; it is not a human label.
 */
#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>

static int read_arg(int argc, char **argv, int index, int fallback) {
    if (index >= argc) {
        return fallback;
    }
    char *end = NULL;
    long value = strtol(argv[index], &end, 10);
    return (end == argv[index]) ? fallback : (int)value;
}
int main(int argc, char **argv) {
    /* RIFT_SOURCE:source_primary */
    int source_primary = read_arg(argc, argv, 1, 9);
    /* RIFT_SOURCE:source_gate */
    int source_gate = read_arg(argc, argv, 2, 1);
    int result = 0;
    if (source_gate != 0 && source_primary > 9) { result = 1; }
    /* RIFT_AP:ap_primary */
    int ap_primary = result;
    printf("AP_primary=%d\n", ap_primary);
    return 0;
}
