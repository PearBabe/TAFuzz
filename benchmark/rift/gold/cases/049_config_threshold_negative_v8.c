/*
 * RIFT-GOLD-049: config_threshold, variant 8, NO_INFLUENCE
 * Generated by RIFT mechanical gold generator 1.0.0.
 * Ground truth is template-derived; it is not a human label.
 */
#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>

static int read_arg(int argc, char **argv, int index, int fallback) {
    if (index >= argc) {
        return fallback;
    }
    char *end = NULL;
    long value = strtol(argv[index], &end, 10);
    return (end == argv[index]) ? fallback : (int)value;
}
int main(int argc, char **argv) {
    /* RIFT_SOURCE:source_configuration */
    int source_configuration = read_arg(argc, argv, 1, 18);
    (void)source_configuration;
    /* RIFT_SOURCE:source_observation */
    int source_observation = read_arg(argc, argv, 2, 18);
    /* RIFT_NODE:node_threshold */
    int node_threshold = 18;
    /* RIFT_NODE:node_observed */
    int node_observed = source_observation;
    /* RIFT_AP:ap_primary */
    int ap_primary = (node_observed >= node_threshold);
    printf("AP_primary=%d\n", ap_primary);
    return 0;
}
