/*
 * RIFT-GOLD-117: joint_inputs, variant 6, MAY_INFLUENCE
 * Generated by RIFT mechanical gold generator 1.0.0.
 * Ground truth is template-derived; it is not a human label.
 */
#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>

static int read_arg(int argc, char **argv, int index, int fallback) {
    if (index >= argc) {
        return fallback;
    }
    char *end = NULL;
    long value = strtol(argv[index], &end, 10);
    return (end == argv[index]) ? fallback : (int)value;
}
int main(int argc, char **argv) {
    /* RIFT_SOURCE:source_left */
    int source_left = read_arg(argc, argv, 1, 10);
    /* RIFT_SOURCE:source_right */
    int source_right = read_arg(argc, argv, 2, 14);
    /* RIFT_SOURCE:source_gate */
    int source_gate = read_arg(argc, argv, 3, 1);
    /* RIFT_NODE:node_joint_guard */
    int node_joint_guard = source_gate && (source_left > 9) && (source_right < 15);
    /* RIFT_AP:ap_primary */
    int ap_primary = node_joint_guard;
    printf("AP_primary=%d\n", ap_primary);
    return 0;
}
